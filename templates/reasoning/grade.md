You are grading the work below on a 5-axis rubric. Score each axis from 0 to 10. Be strict — a 5 is average, a 9 is exceptional, a 10 should almost never appear.

Axes:

ACTIONABILITY (0-10): Does the work give the reader concrete next steps with specific files, commands, or names? Or is it abstract advice? A 10 means a junior could execute it without asking questions. A 3 means it needs a follow-up conversation before any action.

RIGOR (0-10): Are claims backed by specific evidence (quotes, file paths, numbers, prior incidents)? Or are they assertions? A 10 means every claim is sourced. A 3 means the work is mostly opinion.

DEPTH (0-10): Does the work address root causes and second-order effects, or only surface symptoms? A 10 means it traces consequences 2-3 steps out. A 3 means it stops at the obvious.

BREADTH (0-10): Does the work cover the full problem space — assumptions, tradeoffs, failure modes, alternatives? Or does it tunnel on one angle? A 10 means nothing important is missing. A 3 means major gaps.

NOVELTY (0-10): Does the work surface non-obvious findings, or restate things the reader already knows? A 10 means it changes how you think about the problem. A 3 means it tells you what you already knew.

For each axis, provide the score AND a one-sentence justification grounded in a specific part of the work.

Then end with:

TOTAL: <sum>/50
WEAKEST AXIS: <name + score>
SINGLE CHANGE THAT WOULD MOST IMPROVE THIS WORK: <one sentence>

Rules:
- No hedging. Commit to a number.
- Justifications must reference specific content. "The Sprint 1 section gives concrete file paths" is good. "The work is well-organized" is not.
- Do not be polite. A weak work should get weak scores.

Work to grade:

{{INPUT}}
